Activity 3 - Bookshelf

This folder contains the backend (NestJS + TypeORM + SQLite) and frontend (React + Vite) for the Bookshelf activity.

Folders:

- `backend/` - NestJS API with Swagger at `/api`.
- `frontend/` - Vite React app that consumes the API.

See each subfolder README for run instructions.
